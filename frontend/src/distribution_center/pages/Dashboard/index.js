import React from 'react';
import Layout from '~/distribution_center/layout';

const Dashboard = () => {
    return (
        <Layout>
            <div>Home</div>
        </Layout>
    );
};

export default Dashboard;
